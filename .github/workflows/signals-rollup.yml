# Daily k-anonymised rollup of the anonymous play stats.
#
# TWO FACTS ABOUT SCHEDULES, both of which decide when this actually runs:
#
#   1. A scheduled workflow runs ONLY from the repository's DEFAULT branch. This
#      file does nothing at all while it lives on a release branch, so the cron
#      starts working on the day 1.8 merges to main, and not before. Until then
#      use "Run workflow" from the Actions tab, which works from any branch.
#   2. GitHub DISABLES a schedule after 60 days with no repository activity, and
#      emails the owner when it does. A quiet two months stops the rollup
#      silently; re-enable it from the Actions tab.
#
# It also writes NOTHING until `scripts/signals-rollup/config.json` has a
# startDate. Until the owner fills it at the 1.8 cut, every run prints one line
# and exits 0, which also keeps the deploy day's synthetic test rows out of the
# published series.
#
# Output goes to the orphan branch `signals-data`, never to main and never to a
# release branch: main auto-deploys to Pages, and a data commit must never be
# able to publish the game.

name: Signals rollup

on:
  schedule:
    # 03:17 UTC: after the UTC day it rolls up has fully closed, and off the hour,
    # where GitHub's scheduler is most congested.
    - cron: '17 3 * * *'
  workflow_dispatch:

# The job commits to `signals-data`. Nothing else is needed.
permissions:
  contents: write

# Its own group. NOT `pages`: that group serialises the deploy workflow, and a
# rollup joining it would cancel or block a release.
concurrency:
  group: signals-rollup
  cancel-in-progress: false

jobs:
  rollup:
    runs-on: ubuntu-latest
    steps:
      # The script, from the default branch.
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - run: npm ci

      - name: Does the data branch exist yet
        id: data_branch
        run: |
          set -euo pipefail
          if git ls-remote --exit-code --heads origin signals-data >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      # The data branch, in its own folder beside the checkout.
      - name: Check out the data branch
        if: steps.data_branch.outputs.exists == 'true'
        uses: actions/checkout@v5
        with:
          ref: signals-data
          path: signals-data

      # First run only: an ORPHAN branch, so the published totals share no
      # history with the game's source and can never fast-forward into it.
      - name: Create the data branch
        if: steps.data_branch.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          mkdir -p signals-data
          cd signals-data
          git init -q -b signals-data
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          echo "Daily k-anonymised totals from Darling Blades' anonymous play stats. Generated by .github/workflows/signals-rollup.yml; k = 10; see docs/legal/privacy-policy.md." > README.md

      - name: Roll up the missing days
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          # The analytics-scoped token only. There is deliberately no fallback to
          # a deploy token: the T0 spike measured that the deploy token is also
          # accepted by the SQL API, so a fallback would quietly undo the scoping.
          CLOUDFLARE_ANALYTICS_TOKEN: ${{ secrets.CLOUDFLARE_ANALYTICS_TOKEN }}
        run: npx tsx scripts/signals-rollup/run.ts --out signals-data/rollups

      - name: Commit and push what changed
        run: |
          set -euo pipefail
          cd signals-data
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "signals-rollup: nothing new to publish."
            exit 0
          fi
          git commit -q -m "chore(signals): daily rollup"
          # HEAD:signals-data, explicitly. This job can push to exactly one branch.
          git push origin HEAD:signals-data
